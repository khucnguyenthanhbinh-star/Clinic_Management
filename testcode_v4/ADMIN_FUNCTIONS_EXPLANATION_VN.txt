═══════════════════════════════════════════════════════════════════════════════
                    HỆ THỐNG QUẢN LÝ PHÒNG KHÁM - ADMIN
         GIẢI THÍCH CHI TIẾT CƠ CHẾ HOẠT ĐỘNG CÁC CHỨC NĂNG QUẢN TRỊ
═══════════════════════════════════════════════════════════════════════════════

MỤC LỤC:
1. QUẢN LÝ BỆnh NHÂN
2. QUẢN LÝ BÁC SĨ
3. QUẢN LÝ LỊCH HẸN
4. QUẢN LÝ KHO THUỐC
5. QUẢN LÝ TÀI KHOẢN
6. CẤU TRÚC CƠ SỞ DỮ LIỆU
7. QUY TRÌNH LƯU LỮU TRỮ DỮ LIỆU

═══════════════════════════════════════════════════════════════════════════════
1. QUẢN LÝ BỆNH NHÂN (Manage Patients)
═══════════════════════════════════════════════════════════════════════════════

🎯 MỤC ĐÍCH:
   Admin quản lý thông tin của tất cả bệnh nhân trong hệ thống, có khả năng:
   - Xem danh sách tất cả bệnh nhân
   - Tìm kiếm bệnh nhân theo tên hoặc tên đăng nhập
   - Xem chi tiết thông tin bệnh nhân
   - Cập nhật/chỉnh sửa thông tin bệnh nhân
   - Xóa bệnh nhân khỏi hệ thống
   - Tạo mới tài khoản bệnh nhân

📋 CÁC TRƯỜNG THÔNG TIN BỆNH NHÂN:
   - Tên đăng nhập (username): Định danh duy nhất
   - Họ tên (name): Tên đầy đủ của bệnh nhân
   - Ngày sinh (dob): Ngày/tháng/năm sinh
   - Giới tính (gender): Nam/Nữ/Khác
   - Số điện thoại (phone): Liên hệ
   - CCCD: Chứng chỉ căn cước công dân
   - Địa chỉ (address): Nơi cư trú
   - Lịch sử bệnh (history): Bệnh lý nền, dị ứng, bệnh nền,...
   - Bảo hiểm (insurance): Thông tin bảo hiểm y tế

💾 NƠI LƯU TRỮ DỮ LIỆU:
   - Database: clinic.db (SQLite)
   - Bảng: users
   - Cột chính:
     * username (PRIMARY KEY): Định danh bệnh nhân
     * password: Mật khẩu đã mã hóa
     * role: "patient" (cho phép phân biệt với bác sĩ, lễ tân)
     * name: Họ tên
     * info: JSON string chứa tất cả thông tin chi tiết
       (dob, gender, phone, cccd, address, history, insurance)

🔄 QUY TRÌNH HOẠT ĐỘNG:

   A) XEM DANH SÁCH BỆNH NHÂN:
      1. Admin click "Quản lý bệnh nhân"
      2. Hệ thống gọi: controller.db.get_users_by_role("patient")
      3. Database truy vấn SQL: SELECT * FROM users WHERE role = "patient"
      4. Dữ liệu được đưa vào Treeview (bảng hiển thị)
      5. Hiển thị cột: Tên đăng nhập, Họ tên, Điện thoại, Số lịch hẹn
      6. Số lịch hẹn được tính từ bảng appointments

   B) TÌM KIẾM BỆNH NHÂN:
      1. Admin nhập từ khóa vào ô tìm kiếm
      2. Sự kiện "trace" bắt mỗi ký tự nhập vào
      3. Hệ thống gọi filter_patients()
      4. Lọc danh sách: if search_term in username or search_term in name
      5. Chỉ hiển thị bệnh nhân phù hợp

   C) XEM CHI TIẾT BỆNH NHÂN:
      1. Admin chọn 1 bệnh nhân trong bảng
      2. Admin click nút "Xem chi tiết"
      3. Hệ thống lấy username từ dòng được chọn
      4. Gọi: controller.db.get_user(username)
      5. Database thực thi: SELECT * FROM users WHERE username = ?
      6. Trả về object user_data với đầy đủ thông tin
      7. Parse JSON từ cột "info" để lấy chi tiết (dob, phone, address,...)
      8. Hiển thị trong popup messagebox với định dạng đẹp

   D) CẬP NHẬT THÔNG TIN BỆNH NHÂN:
      1. Admin chọn bệnh nhân và click "Cập nhật thông tin"
      2. Tạo cửa sổ mới (Toplevel window)
      3. Hiển thị form với các trường:
         - Họ tên (name)
         - Ngày sinh (dob)
         - Giới tính (gender)
         - Số điện thoại (phone)
         - CCCD (cccd)
         - Địa chỉ (address)
         - Lịch sử bệnh (history)
      4. Admin sửa các giá trị cần thiết
      5. Click "Lưu" → Thực thi SQL UPDATE:
         UPDATE users SET name = ?, info = ? WHERE username = ?
      6. Dữ liệu mới được lưu vào database
      7. Đóng cửa sổ và refresh danh sách

   E) XÓA BỆNH NHÂN:
      1. Admin chọn bệnh nhân và click "Xóa"
      2. Hiển thị hộp xác nhận: "Bạn chắc chắn muốn xóa?"
      3. Nếu đồng ý, thực thi: DELETE FROM users WHERE username = ?
      4. Xóa tất cả dữ liệu liên quan (lịch hẹn, hóa đơn)
      5. Refresh danh sách

   F) TẠO MỚI BỆNH NHÂN:
      1. Admin click "Tạo mới"
      2. Mở cửa sổ form đăng ký
      3. Admin nhập thông tin: username, password, name, các chi tiết khác
      4. Click "Tạo" → Gọi: controller.db.add_user()
      5. Hệ thống mã hóa password bằng PasswordHasher
      6. Chèn record mới vào users table
      7. Insert vào: (username, hashed_password, "patient", name, JSON info)
      8. Refresh danh sách

═══════════════════════════════════════════════════════════════════════════════
2. QUẢN LÝ BÁC SĨ (Manage Doctors)
═══════════════════════════════════════════════════════════════════════════════

🎯 MỤC ĐÍCH:
   Admin quản lý thông tin bác sĩ, bao gồm:
   - Xem danh sách bác sĩ
   - Tìm kiếm bác sĩ
   - Xem chi tiết hồ sơ bác sĩ (chuyên khoa, kinh nghiệm, giá khám,...)
   - Cập nhật thông tin bác sĩ
   - Xóa bác sĩ
   - Tạo tài khoản bác sĩ mới

📋 CÁC TRƯỜNG THÔNG TIN BÁC SĨ:
   - Tên đăng nhập (username): Định danh
   - Họ tên (name): Tên bác sĩ
   - Chuyên khoa (specialty): Nhi, Tim mạch, Ngoại, Phụ khoa,...
   - Chi nhánh (branch): Cơ sở làm việc
   - Kinh nghiệm (exp): Số năm kinh nghiệm
   - Đánh giá (rating): Số sao (1-5)
   - Giá khám (price): Chi phí khám bệnh
   - Số điện thoại (phone): Liên lạc
   - Hình ảnh (image): Ảnh đại diện

💾 NƠI LƯU TRỮ:
   - Database: clinic.db
   - Bảng: users với role = "doctor"
   - Cột "info" lưu JSON: {specialty, branch, exp, rating, price, phone, image}

🔄 QUY TRÌNH HOẠT ĐỘNG:

   A) XEM DANH SÁCH BÁC SĨ:
      1. Admin click "Quản lý bác sĩ"
      2. Gọi: controller.db.get_users_by_role("doctor")
      3. SELECT * FROM users WHERE role = "doctor"
      4. Tráo Treeview: Username, Họ tên, Chuyên khoa, Chi nhánh, Số lịch hẹn
      5. Số lịch hẹn được đếm từ bảng appointments

   B) TÌM KIẾM BÁC SĨ:
      1. Admin nhập từ khóa
      2. Hệ thống filter_doctors() tìm kiếm trong username hoặc name
      3. Hiển thị kết quả phù hợp

   C) XEM CHI TIẾT BÁC SĨ:
      1. Admin chọn bác sĩ → click "Xem chi tiết"
      2. Lấy username từ dòng được chọn
      3. controller.db.get_user(username)
      4. Parse JSON từ "info" field
      5. Hiển thị popup:
         - Tên đăng nhập
         - Họ tên
         - Chuyên khoa
         - Chi nhánh
         - Kinh nghiệm (năm)
         - Đánh giá (⭐)
         - Giá khám (đ)
         - Số điện thoại
         - Hình ảnh

   D) CẬP NHẬT BÁC SĨ:
      1. Admin chọn bác sĩ → "Cập nhật thông tin"
      2. Mở form edit với tất cả trường (name, specialty, branch, exp, rating, price, phone, image)
      3. Admin chỉnh sửa → click "Lưu"
      4. UPDATE users SET name = ?, info = ? WHERE username = ?
      5. Refresh danh sách

   E) XÓA BÁC SĨ:
      1. Admin chọn → "Xóa bác sĩ"
      2. Xác nhận lần nữa
      3. DELETE FROM users WHERE username = ?
      4. Xóa lịch hẹn liên quan

   F) TẠO BÁC SĨ MỚI:
      1. Admin click "Tạo mới"
      2. Mở form: username, password, name, specialty, branch, exp, rating, price, phone, image
      3. Click "Tạo" → add_user(username, password, "doctor", name, JSON info)
      4. Mật khẩu được mã hóa trước khi lưu
      5. Insert vào users table với role = "doctor"

═══════════════════════════════════════════════════════════════════════════════
3. QUẢN LÝ LỊCH HẸN (Manage Appointments)
═══════════════════════════════════════════════════════════════════════════════

🎯 MỤC ĐÍCH:
   Admin quản lý toàn bộ lịch hẹn của khách phòng khám:
   - Xem tất cả lịch hẹn
   - Lọc theo trạng thái (Đã đặt, Checked-in, Hoàn thành, Đã hủy)
   - Xem chi tiết lịch hẹn
   - Thay đổi trạng thái lịch hẹn
   - Xóa lịch hẹn
   - Tìm kiếm lịch hẹn

📋 CÁC TRƯỜNG THÔNG TIN LỊCH HẸN:
   - ID: Định danh lịch hẹn
   - Patient (Bệnh nhân): username bệnh nhân
   - Ngày (date): Ngày hẹn (YYYY-MM-DD)
   - Giờ (time): Giờ hẹn (HH:MM)
   - Lý do/Ghi chú (reason): Lý do khám, ghi chú của bác sĩ
   - Trạng thái (status): Đã đặt / Checked-in / Hoàn thành / Đã hủy

💾 NƠI LƯU TRỮ:
   - Database: clinic.db
   - Bảng: appointments
   - Cột chính:
     * id (PRIMARY KEY AUTOINCREMENT): ID tự động tăng
     * patient_username (FOREIGN KEY): Liên kết tới users.username
     * date: Ngày hẹn
     * time: Giờ hẹn
     * reason: Lý do/ghi chú
     * status: Trạng thái

🔄 QUY TRÌNH HOẠT ĐỘNG:

   A) XEM DANH SÁCH LỊCH HẸN:
      1. Admin click "Quản lý lịch hẹn"
      2. Gọi: controller.db.get_appointments()
      3. SELECT * FROM appointments
      4. Treeview hiển thị các cột: ID, Bệnh nhân, Ngày, Giờ, Lý do, Trạng thái
      5. Tên bệnh nhân được lấy từ users table: get_user(patient_username)

   B) LỌC THEO TRẠNG THÁI:
      1. Admin chọn trạng thái từ dropdown: "Tất cả", "Đã đặt", "Checked-in", "Hoàn thành", "Đã hủy"
      2. Hệ thống refresh_list() với bộ lọc
      3. Nếu status_filter != "Tất cả", chỉ hiển thị lịch hẹn có status = status_filter
      4. Còn lại hiển thị tất cả

   C) XEM CHI TIẾT LỊCH HẸN:
      1. Admin chọn 1 lịch hẹn → click "Xem chi tiết"
      2. Lấy appointment_id từ dòng được chọn
      3. Tìm trong appointments table: SELECT * FROM appointments WHERE id = ?
      4. Hiển thị popup với tất cả thông tin:
         - ID lịch hẹn
         - Tên bệnh nhân (lookup từ users)
         - Ngày/giờ hẹn
         - Lý do/ghi chú
         - Trạng thái hiện tại

   D) THAY ĐỔI TRẠNG THÁI LỊCH HẸN:
      1. Admin chọn lịch hẹn → click "Thay đổi trạng thái"
      2. Mở dialog chọn trạng thái mới (dropdown)
      3. Các trạng thái có sẵn: Đã đặt → Checked-in → Hoàn thành
      4. Hoặc có thể chuyển sang "Đã hủy"
      5. Click "OK" → UPDATE appointments SET status = ? WHERE id = ?
      6. Status được cập nhật trong database
      7. Refresh danh sách

   E) XÓA LỊCH HẸN:
      1. Admin chọn → click "Xóa"
      2. Xác nhận: "Bạn chắc chắn muốn xóa lịch hẹn này?"
      3. DELETE FROM appointments WHERE id = ?
      4. Lịch hẹn bị xóa vĩnh viễn
      5. Refresh danh sách

   F) GHI CHUYÊN PHÁT TRIỂN - TÌM KIẾM:
      - Hiện tại chưa có thanh tìm kiếm
      - Có thể thêm: tìm theo tên bệnh nhân, ngày, hoặc trạng thái

═══════════════════════════════════════════════════════════════════════════════
4. QUẢN LÝ KHO THUỐC (Manage Medicines)
═══════════════════════════════════════════════════════════════════════════════

🎯 MỤC ĐÍCH:
   Admin quản lý kho dược phẩm của phòng khám:
   - Xem danh sách thuốc trong kho
   - Nhập thuốc (tăng số lượng)
   - Xuất thuốc (giảm số lượng)
   - Tìm kiếm thuốc
   - Chỉnh sửa thông tin thuốc
   - Xóa thuốc khỏi kho

📋 CÁC TRƯỜNG THÔNG TIN THUỐC:
   - Tên thuốc (medicine): Định danh duy nhất, ví dụ "Aspirin 500mg"
   - Số lượng (quantity): Số lượng hiện có trong kho
   - Đơn vị (unit): Viên, lọ, hộp, lần, ml,...

💾 NƠI LƯU TRỮ:
   - Database: clinic.db
   - Bảng: medicines
   - Cột chính:
     * name (PRIMARY KEY): Tên thuốc (duy nhất)
     * quantity (INTEGER): Số lượng
     * unit (TEXT): Đơn vị tính

🔄 QUY TRÌNH HOẠT ĐỘNG:

   A) XEM DANH SÁCH THUỐC:
      1. Admin click "Quản lý kho thuốc"
      2. Gọi: controller.db.get_medicines()
      3. SELECT * FROM medicines
      4. Trả về dictionary: {name: {quantity, unit}, ...}
      5. Hiển thị Treeview: Tên thuốc, Số lượng, Đơn vị

   B) NHẬP THUỐC VÀO KHO:
      1. Admin nhập thông tin:
         - Tên thuốc (ví dụ: "Paracetamol 500mg")
         - Số lượng (ví dụ: 100)
         - Đơn vị (ví dụ: "Viên")
      2. Click "Nhập kho"
      3. Hệ thống kiểm tra: Thuốc này đã tồn tại chưa?
      4. Nếu TỒN TẠI (SELECT quantity FROM medicines WHERE name = ?):
         - Cộng thêm: new_qty = current_qty + input_qty
         - UPDATE medicines SET quantity = ? WHERE name = ?
      5. Nếu CHƯA TỒN TẠI:
         - INSERT INTO medicines VALUES (?, ?, ?)
         - Thêm record mới
      6. Refresh danh sách và xóa form
      7. Hiển thị thông báo: "Nhập kho thành công!"

   C) XUẤT THUỐC KHỎI KHO:
      1. Admin nhập thông tin thuốc và số lượng cần xuất
      2. Click "Xóa khỏi kho"
      3. Hệ thống kiểm tra: Số lượng hiện có có đủ không?
      4. Nếu KHÔNG ĐỦ: Hiển thị cảnh báo "Số lượng không đủ"
      5. Nếu ĐỦ:
         - new_qty = current_qty - output_qty
         - UPDATE medicines SET quantity = ? WHERE name = ?
      6. Nếu số lượng = 0, có thể giữ nguyên hoặc xóa
      7. Refresh danh sách

   D) TÌM KIẾM THUỐC:
      1. Admin nhập từ khóa vào ô "Tìm kiếm"
      2. Sự kiện trace bắt mỗi ký tự
      3. filter_medicines() được gọi
      4. Lọc: if search_term in medicine_name.lower()
      5. Chỉ hiển thị thuốc phù hợp

   E) CHỈNH SỬA THUỐC:
      1. Admin chọn thuốc trong danh sách → click "Chỉnh sửa"
      2. Mở cửa sổ edit
      3. Hiển thị form: Tên (có thể không sửa), Số lượng, Đơn vị
      4. Admin sửa thông tin → click "Lưu"
      5. UPDATE medicines SET quantity = ?, unit = ? WHERE name = ?
      6. Refresh danh sách

   F) XÓA THUỐC:
      1. Admin chọn thuốc → click "Xóa"
      2. Xác nhận: "Bạn chắc chắn muốn xóa?"
      3. DELETE FROM medicines WHERE name = ?
      4. Thuốc bị xóa khỏi kho
      5. Refresh danh sách

═══════════════════════════════════════════════════════════════════════════════
5. QUẢN LÝ TÀI KHOẢN (Manage Users)
═══════════════════════════════════════════════════════════════════════════════

🎯 MỤC ĐÍCH:
   Admin quản lý toàn bộ tài khoản trong hệ thống:
   - Xem danh sách tất cả người dùng (Admin, Bác sĩ, Bệnh nhân, Lễ tân)
   - Tìm kiếm người dùng
   - Xem chi tiết tài khoản
   - Đổi mật khẩu cho người dùng
   - Tạo tài khoản mới
   - Xóa tài khoản

📋 CÁC TRƯỜNG THÔNG TIN NGƯỜI DÙNG:
   - Tên đăng nhập (username): Định danh duy nhất
   - Mật khẩu (password): Đã mã hóa bằng hashing
   - Họ tên (name): Tên đầy đủ
   - Vai trò (role): "admin", "doctor", "patient", "receptionist"
   - Thông tin bổ sung (info): JSON string (tùy theo vai trò)

💾 NƠI LƯU TRỮ:
   - Database: clinic.db
   - Bảng: users
   - Cột chính:
     * username (PRIMARY KEY): Định danh
     * password: Mật khẩu đã hash
     * role: Vai trò
     * name: Họ tên
     * info: JSON (nội dung tùy vai trò)

🔄 QUY TRÌNH HOẠT ĐỘNG:

   A) XEM DANH SÁCH TÀI KHOẢN:
      1. Admin click "Quản lý tài khoản"
      2. Gọi: controller.db.get_all_users()
      3. SELECT * FROM users
      4. Trả về: {username: {name, role}, ...}
      5. Treeview hiển thị: Tên đăng nhập, Họ tên, Vai trò (Admin/Bác sĩ/Bệnh nhân/Lễ tân)

   B) TÌM KIẾM NGƯỜI DÙNG:
      1. Admin nhập từ khóa
      2. filter_users() được gọi
      3. Lọc: if search_term in username.lower() or search_term in name.lower()
      4. Hiển thị kết quả phù hợp

   C) XEM CHI TIẾT TÀI KHOẢN:
      1. Admin chọn người dùng → click "Xem chi tiết"
      2. Lấy username từ dòng được chọn
      3. controller.db.get_user(username)
      4. Hiển thị popup với thông tin:
         - Tên đăng nhập
         - Họ tên
         - Vai trò (Admin/Bác sĩ/Bệnh nhân/Lễ tân)
         - Thông tin bổ sung (tùy vai trò)
      5. Có thể hiển thị ngày tạo tài khoản (nếu có)

   D) ĐỔI MẬT KHẨU:
      1. Admin chọn người dùng → click "Đổi mật khẩu"
      2. Mở dialog:
         - Nhập mật khẩu mới
         - Xác nhận mật khẩu mới
      3. Admin nhập thông tin → click "OK"
      4. Hệ thống validate:
         - Mật khẩu phải >= 6 ký tự
         - Hai lần nhập phải giống nhau
      5. Gọi PasswordHasher.hash_password() để mã hóa
      6. UPDATE users SET password = ? WHERE username = ?
      7. Mật khẩu mới được lưu vào database
      8. Hiển thị "Đổi mật khẩu thành công!"

   E) TẠO TÀI KHOẢN MỚI:
      1. Admin click "Tạo tài khoản"
      2. Mở cửa sổ form đăng ký
      3. Hiển thị trường:
         - Tên đăng nhập (username)
         - Mật khẩu (password)
         - Họ tên (name)
         - Vai trò (role) - dropdown: Admin, Bác sĩ, Bệnh nhân, Lễ tân
         - Thông tin bổ sung (tùy vai trò)
      4. Admin nhập → click "Tạo"
      5. Hệ thống validate:
         - Username không được để trống
         - Mật khẩu >= 6 ký tự
         - Username chưa tồn tại
      6. PasswordHasher.hash_password() mã hóa mật khẩu
      7. INSERT INTO users (username, password, role, name, info) VALUES (...)
      8. Tài khoản mới được tạo
      9. Refresh danh sách
      10. Hiển thị "Tạo tài khoản thành công!"

   F) XÓA TÀI KHOẢN:
      1. Admin chọn người dùng → click "Xóa tài khoản"
      2. Xác nhận lần nữa: "Bạn chắc chắn muốn xóa?"
      3. Cảnh báo: Dữ liệu liên quan cũng sẽ bị xóa
      4. DELETE FROM users WHERE username = ?
      5. Tất cả lịch hẹn, hóa đơn của người dùng cũng bị xóa (nếu có ràng buộc CASCADE)
      6. Refresh danh sách

═══════════════════════════════════════════════════════════════════════════════
6. CẤU TRÚC CƠ SỞ DỮ LIỆU
═══════════════════════════════════════════════════════════════════════════════

BẢNG 1: USERS (Người dùng)
┌─────────────────────────────────────────────────────────────────┐
│ Cột         │ Kiểu dữ liệu │ Ràng buộc  │ Mô tả                 │
├─────────────────────────────────────────────────────────────────┤
│ username    │ TEXT         │ PRIMARY KEY│ Tên đăng nhập          │
│ password    │ TEXT         │ NOT NULL   │ Mật khẩu (đã hash)     │
│ role        │ TEXT         │ NOT NULL   │ admin/doctor/...       │
│ name        │ TEXT         │ NOT NULL   │ Tên đầy đủ             │
│ info        │ TEXT         │ -          │ JSON (chi tiết)        │
└─────────────────────────────────────────────────────────────────┘

BẢNG 2: MEDICINES (Thuốc)
┌─────────────────────────────────────────────────────────────────┐
│ Cột      │ Kiểu dữ liệu │ Ràng buộc  │ Mô tả                    │
├─────────────────────────────────────────────────────────────────┤
│ name     │ TEXT         │ PRIMARY KEY│ Tên thuốc                │
│ quantity │ INTEGER      │ -          │ Số lượng trong kho       │
│ unit     │ TEXT         │ -          │ Đơn vị tính              │
└─────────────────────────────────────────────────────────────────┘

BẢNG 3: APPOINTMENTS (Lịch hẹn)
┌─────────────────────────────────────────────────────────────────┐
│ Cột              │ Kiểu dữ liệu │ Ràng buộc           │ Mô tả    │
├─────────────────────────────────────────────────────────────────┤
│ id               │ INTEGER      │ PRIMARY KEY AUTO    │ ID tự tăng│
│ patient_username │ TEXT         │ FOREIGN KEY         │ Link users│
│ date             │ TEXT         │ -                   │ Ngày hẹn  │
│ time             │ TEXT         │ -                   │ Giờ hẹn   │
│ reason           │ TEXT         │ -                   │ Lý do     │
│ status           │ TEXT         │ -                   │ Trạng thái│
└─────────────────────────────────────────────────────────────────┘

BẢNG 4: INVOICES (Hóa đơn)
┌─────────────────────────────────────────────────────────────────┐
│ Cột              │ Kiểu dữ liệu │ Ràng buộc           │ Mô tả    │
├─────────────────────────────────────────────────────────────────┤
│ id               │ INTEGER      │ PRIMARY KEY AUTO    │ ID hóa đơn│
│ patient_username │ TEXT         │ FOREIGN KEY         │ Link users│
│ service_name     │ TEXT         │ -                   │ Dịch vụ   │
│ amount           │ INTEGER      │ -                   │ Số tiền   │
│ status           │ TEXT         │ -                   │ Paid/Unpaid│
│ created_at       │ TEXT         │ -                   │ Thời gian │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
7. QUY TRÌNH LƯU TRỮ DỮ LIỆU TỔNG QUÁT
═══════════════════════════════════════════════════════════════════════════════

📍 TỰ ĐỘNG ĐĈ TRONG CÁC THAO TÁC:

1. BỆNH NHÂN ĐẶT LỊCH HẸN:
   Bệnh nhân (trong giao diện Patient) → Click "Đặt lịch hẹn"
   → Chọn bác sĩ, ngày, giờ, lý do
   → Click "Xác nhận"
   → controller.db.add_appointment(username, date, time, reason, "Đã đặt")
   → INSERT INTO appointments (...) VALUES (...)
   → Dữ liệu lưu vào database
   → Status: "Đã đặt"

2. BỆNH NHÂN ĐẾN PHÒNG KHÁM:
   Lễ tân (Receptionist) → Check-in lịch hẹn
   → UPDATE appointments SET status = "Checked-in" WHERE id = ?
   → Status thay đổi

3. BÁC SĨ KHÁM BỆNH NHÂN:
   Bác sĩ → Xem bệnh nhân, làm phiếu khám
   → Ghi chú kết quả, kê đơn thuốc
   → Lưu vào appointments (cột reason)
   → UPDATE appointments SET reason = ?, status = "Hoàn thành"
   → Status thay đổi thành "Hoàn thành"

4. TỰ ĐỘNG TẠO HÓA ĐƠN:
   Khi khám xong → Tính chi phí
   → controller.db.add_invoice(username, service, amount, "Unpaid")
   → INSERT INTO invoices (...) VALUES (...)
   → Hóa đơn được lưu

5. BỆNH NHÂN THANH TOÁN:
   Bệnh nhân → Click "Thanh toán"
   → Chọn hóa đơn
   → Click "Thanh toán"
   → controller.db.pay_invoice(invoice_id)
   → UPDATE invoices SET status = "Paid" WHERE id = ?
   → Status: "Paid"

═══════════════════════════════════════════════════════════════════════════════
TÓMLƯỢC DÒNG CÔNG KHAI CHỨC NĂNG ADMIN
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ CHỨC NĂNG          │ BẢNG TÀI LIỆU    │ CÁC THAO TÁC          │
├─────────────────────────────────────────────────────────────────┤
│ Quản lý bệnh nhân  │ users            │ CRUD (tạo, xem,       │
│                    │ appointments     │ sửa, xóa)             │
├─────────────────────────────────────────────────────────────────┤
│ Quản lý bác sĩ     │ users            │ CRUD                  │
│                    │ appointments     │ (+ chuyên khoa, giá)  │
├─────────────────────────────────────────────────────────────────┤
│ Quản lý lịch hẹn   │ appointments     │ Xem, lọc, thay đổi    │
│                    │ users           │ trạng thái, xóa       │
├─────────────────────────────────────────────────────────────────┤
│ Quản lý kho thuốc  │ medicines        │ Nhập, xuất, tìm,      │
│                    │                  │ chỉnh sửa, xóa        │
├─────────────────────────────────────────────────────────────────┤
│ Quản lý tài khoản  │ users            │ CRUD + đổi mật khẩu   │
│                    │ invoices         │                       │
│                    │ appointments     │                       │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
GHI CHÚ QUAN TRỌNG
═══════════════════════════════════════════════════════════════════════════════

1. ĐỊA ĐIỂM CƠ SỞ DỮ LIỆU:
   📁 Đường dẫn: /database/clinic.db
   ✓ Tự động tạo khi chạy chương trình lần đầu
   ✓ Tất cả dữ liệu được lưu tại đây

2. MẬT KHẨU:
   🔐 Được mã hóa bằng PasswordHasher (trong core/password_hasher.py)
   ✓ Không bao giờ lưu mật khẩu gốc
   ✓ Khi đăng nhập, so sánh hash không so sánh text

3. JSON TRONG "INFO":
   📝 Cột "info" chứa JSON string với thông tin chi tiết
   ✓ Dễ dàng mở rộng mà không thay đổi cơ sở dữ liệu
   ✓ Ví dụ: {"dob": "1990-05-15", "phone": "0912345678", ...}

4. FOREIGN KEY:
   🔗 appointments.patient_username → users.username
   ✓ Đảm bảo tính toàn vẹn dữ liệu
   ✓ Xóa bệnh nhân sẽ tự động xóa lịch hẹn liên quan (nếu có CASCADE)

5. GIAO DIỆN:
   🎨 Sử dụng tkinter (Python GUI framework)
   ✓ Treeview để hiển thị danh sách
   ✓ Form với Entry widgets để nhập dữ liệu
   ✓ Messagebox cho thông báo và xác nhận

═══════════════════════════════════════════════════════════════════════════════
KẾT LUẬN
═══════════════════════════════════════════════════════════════════════════════

Hệ thống Quản lý phòng khám sử dụng mô hình CRUD (Create, Read, Update, Delete)
để quản lý tất cả dữ liệu. Mỗi chức năng Admin đều thực hiện các thao tác cơ bản
này trên các bảng dữ liệu khác nhau. Dữ liệu được lưu trữ tập trung trong SQLite
database (clinic.db), cho phép dễ dàng truy cập, sao lưu, và mở rộng trong tương lai.

═══════════════════════════════════════════════════════════════════════════════
